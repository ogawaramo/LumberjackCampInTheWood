# PRD: 森のきこりキャンプ（VRChatワールド）
**Repository Planning Graph (RPG) 方式準拠**

**最終更新**: 2025-11-16
**プラットフォーム**: VRChat（PC / Quest対応）
**技術スタック**: Unity 2022.3.22f1 / SDK 3.9.0 / UdonSharp 1.1.9

---

## 📋 概要（Overview）

### Problem Statement

VRChatには多様なワールドが存在するが、以下の課題が未解決である：

1. **長時間滞在のモチベーション不足**
   - 観光型ワールド: 一度訪問したら飽きる（平均滞在10分以下）
   - ゲーム型ワールド: 対戦やクリアが必須で疲れる
   - 交流特化型: 会話ネタがないと気まずい

2. **単調な「ながら作業」の欠如**
   - 会話しながらできる軽作業が少ない
   - Valheimのようなcozyな共同作業体験がVRChat内にない
   - 作業の達成感 + 自然な会話の両立が困難

3. **継続プレイへの動機付けの弱さ**
   - スキル成長、コレクション、見せびらかし要素が少ない
   - Persistenceを活用したワールドが限定的（技術提供: SDK 3.7.4、2024年11月）

4. **緊張感・メリハリの欠如**
   - まったり作業だけでは単調になりがち
   - 協力プレイの必然性が弱い（1人でも完結してしまう）
   - Don't Starve や Valheim のような「たまの危機」が作業のメリハリを生む

### Target Users

**ペルソナ**:
- **Valheim/Minecraft愛好家**（採集+クラフトが好き）
- **Palia的cozy MMOプレイヤー**（戦闘より生活系コンテンツを好む）
- **VR雑談勢**（ガチ対戦より「だらっとVCで作業」したい層）

**ユーザーワークフロー**:
1. VRChatで気軽に集まる → 森のきこりキャンプに入る
2. 木を切る・運ぶ作業をしながら会話
3. スキルレベル・コインが貯まる → コスメ解禁
4. トロフィー棚で「やり込み度」を見せびらかす
5. 次回も「続きの成長」を楽しみに訪問

### Success Metrics

| KPI | 目標値 | 測定方法 |
|-----|--------|---------|
| **平均滞在時間** | 20〜40分/訪問 | Udonイベントログ（入退室時刻） |
| **リピート率** | 1週間以内の再訪30%以上 | PlayerData参照（最終訪問日時保存） |
| **同時接続** | 1インスタンス平均4〜12人 | VRChat Analytics API |
| **スキルレベル到達率** | Lv10到達率50%（1ヶ月以内） | Persistence統計 |
| **コスメ購入率** | ユニークプレイヤーの70%が1点以上購入 | PlayerData統計 |
| **熊撃退成功率** | 60%以上（協力プレイ時80%） | 熊イベント統計（撃退/総襲撃回数） |
| **Combat参加率** | 襲撃時の50%が戦闘参加（残り50%は逃走） | 行動ログ分析 |

---

## 🌲 機能分解（Functional Decomposition）

### Capability: 伐採システム（Woodcutting）

プレイヤーが木を切り倒し、丸太を獲得する基本サイクル

#### Feature: 立木伐採
- **Description**: 斧で木を叩いてHP削減し、倒木状態へ遷移させる
- **Inputs**:
  - プレイヤーの斧スイングアクション（トリガー入力）
  - 木オブジェクトの当たり判定（コライダー）
- **Outputs**:
  - 木のHP減少量
  - 倒木アニメーション（HP=0時）
  - 協力伐採ボーナス判定（2人以上が一定時間内に叩いた場合）
- **Behavior**:
  1. 斧コライダーと木コライダーの衝突検知
  2. ダメージ計算（基礎値 + スキルレベル補正）
  3. HP更新（UdonSyncedで全プレイヤー共有）
  4. HP=0 → 倒木状態に遷移（最後の一撃方向に倒れる）

#### Feature: 倒木分解
- **Description**: 倒れた木をさらに叩いて丸太オブジェクトをスポーン
- **Inputs**:
  - 倒木オブジェクト
  - プレイヤーの斧スイング
- **Outputs**:
  - 丸太オブジェクト（小丸太 or 太丸太）複数個
  - 枝オブジェクト（副産物）
  - スキルXP付与
- **Behavior**:
  1. 倒木のHPを再設定（立木の50%程度）
  2. HP=0で丸太オブジェクトを指定位置にInstantiate
  3. 木の種類・プレイヤースキルに応じて丸太数を決定
  4. 倒木オブジェクトを非アクティブ化

#### Feature: 木のリスポーン
- **Description**: 伐採済みの木を一定時間後に再生成
- **Inputs**:
  - 伐採済み木の位置情報
  - Camp Rankレベル（リスポーン速度に影響）
- **Outputs**:
  - 新しい立木オブジェクト
  - リスポーン演出（パーティクル等）
- **Behavior**:
  1. 伐採完了時にタイマー開始（300秒基準）
  2. Camp Rank補正でタイマー短縮（例: Rank5で-20%）
  3. タイマー完了後、元の位置に木を再生成
  4. 同期処理（Master Client管理）

---

### Capability: 運搬システム（Carrying）

丸太をキャンプまで協力して運ぶインタラクション

#### Feature: 小丸太運搬
- **Description**: 1人で持てる軽量丸太の運搬
- **Inputs**:
  - VRC_Pickupコンポーネント
  - プレイヤーのグリップ入力
- **Outputs**:
  - プレイヤー移動速度デバフ（-15%）
  - 運搬距離ログ（スキルXP計算用）
- **Behavior**:
  1. 丸太をPickupで掴む
  2. 保持中の移動速度を playerSpeed * 0.85 に設定
  3. 移動距離を内部カウント
  4. キャンプエリアの納品ゾーンで離すとカウント完了

#### Feature: 太丸太協力運搬
- **Description**: 2人以上で同時に持つ必要がある重量丸太
- **Inputs**:
  - 複数のVRC_Pickup（ハンドルポイント）
  - 2人以上のプレイヤーグリップ入力
- **Outputs**:
  - 丸太の移動（中間位置基準）
  - 協力ボーナスXP
  - 運搬成功フラグ（実績用）
- **Behavior**:
  1. 太丸太に2つ以上のハンドルポイント配置
  2. 同時に2人が掴んだ場合、丸太の位置を両者の中間点に補間
  3. 入力ベクトルの平均値で移動方向決定
  4. 1人が離すと丸太が重くなり移動不可

#### Feature: 運搬距離計測
- **Description**: プレイヤーの運搬スキル向上のための距離トラッキング
- **Inputs**:
  - 丸太保持開始位置（Vector3）
  - 丸太離した位置（Vector3）
- **Outputs**:
  - 運搬距離（float、メートル単位）
  - CarryingスキルXP加算
- **Behavior**:
  1. Pickup時に開始位置を保存
  2. Drop時に終了位置との距離計算
  3. 距離 × 丸太重量係数 = XP
  4. デイリーランキングに距離記録

---

### Capability: 加工・クラフトシステム（Processing & Crafting）

丸太を板材に変換し、キャンプ設備を建築

#### Feature: 切断台での基本加工
- **Description**: 丸太を板材や小丸太に変換する設備
- **Inputs**:
  - 丸太オブジェクト（設備上に配置）
  - プレイヤーのボタン押下
- **Outputs**:
  - 板材オブジェクト（2枚）
  - 小丸太オブジェクト（小丸太投入時）
  - 枝オブジェクト
- **Behavior**:
  1. 丸太を切断台コライダー内に配置
  2. UIボタン表示（「加工開始」）
  3. 3秒のプログレスバー表示
  4. 完了時、丸太を削除し変換後アイテムをスポーン

#### Feature: 製材所での大量加工
- **Description**: Camp Rank解禁で利用可能な高速加工設備
- **Inputs**:
  - 太丸太 × 複数
  - 加工時間（10秒）
- **Outputs**:
  - 板材 × 大量（通常の2倍効率）
  - CraftingスキルXP（大量付与）
- **Behavior**:
  1. Camp Rank 5以上で解禁
  2. 太丸太を複数投入可能なインベントリ
  3. 一括処理で効率的に板材生成
  4. 処理中は他プレイヤーと会話する時間を提供

#### Feature: キャンプ建築
- **Description**: 板材を使って設備・装飾をクラフト
- **Inputs**:
  - 板材 × 指定数
  - 建築ポイント（predefined位置）
- **Outputs**:
  - 新しい設備オブジェクト（作業台、ベンチなど）
  - Camp XP加算
  - クラフト実績カウント
- **Behavior**:
  1. 建築可能ポイントに近づくとUIパネル表示
  2. 必要素材チェック（PlayerData参照）
  3. 素材消費 → オブジェクトアクティブ化
  4. Camp Rankへの寄与を記録

---

### Capability: 成長システム（Progression）

個人スキル・キャンプランクの成長管理

#### Feature: ライフスキル経験値管理
- **Description**: Woodcutting/Carrying/CraftingスキルのXP・レベル計算
- **Inputs**:
  - 行動ログ（伐採、運搬、クラフト）
  - 獲得XP量
- **Outputs**:
  - 現在XP / 次レベルまでのXP
  - レベルアップイベント
  - パッシブ効果更新
- **Behavior**:
  1. 行動ごとにXPを PlayerData["Woodcutting_XP"] に加算
  2. レベル計算式: XP >= (Lv * 100 + Lv^2 * 10)
  3. レベルアップ時、HUD演出 + パッシブ効果反映
  4. Persistenceで永続化（PlayerData、100KB制限考慮）

#### Feature: スキルレベル効果適用
- **Description**: レベルに応じたパッシブ効果（ダメージ増加、速度向上など）
- **Inputs**:
  - スキルレベル（int）
  - スキル種別（enum: Woodcutting/Carrying/Crafting）
- **Outputs**:
  - 補正値（float、1.0基準で増加）
  - UI表示（スキル詳細パネル）
- **Behavior**:
  1. WoodcuttingレベルでダメージBase * (1 + Lv * 0.05) 計算
  2. Carryingレベルで移動速度デバフ軽減（0.85 → 0.90など）
  3. Craftingレベルで素材消費-5%、副産物+10%
  4. レベル6で分岐選択（パワー派 / サポート派）

#### Feature: Camp Rank管理
- **Description**: インスタンス共有の拠点レベル（エリア解放、レシピ解放）
- **Inputs**:
  - Camp XP（全プレイヤーの貢献合計）
  - インスタンス開始時の初期化
- **Outputs**:
  - Camp Rank（int、1〜10）
  - 解放済みエリア・レシピリスト
- **Behavior**:
  1. インスタンス開始時に Camp Rank = 1、XP = 0 で初期化
  2. 木材納品、建築完了で XP 加算（UdonSynced変数）
  3. Rank Up時、奥の森エリア開放 or 新レシピ追加
  4. **注意**: インスタンス単位で管理（PlayerData非保存）

---

### Capability: 通貨・交換システム（Economy）

個人通貨・キャンプ通貨の獲得と使用

#### Feature: きこりコイン獲得
- **Description**: 行動報酬として個人通貨を付与
- **Inputs**:
  - 行動完了イベント（伐採、運搬、クエスト達成）
- **Outputs**:
  - コイン増加量（int）
  - PlayerData["WoodcutterCoins"] 更新
- **Behavior**:
  1. 木1本伐採 = +5コイン
  2. 丸太運搬1回 = +3コイン
  3. クエスト達成 = +50〜200コイン
  4. 獲得時にHUD通知（「+5コイン獲得！」）

#### Feature: コスメショップ
- **Description**: きこりコインで斧スキン、帽子、アニメーションを購入
- **Inputs**:
  - プレイヤーの所持コイン
  - 購入希望アイテムID
- **Outputs**:
  - コイン消費
  - PlayerData["UnlockedCosmetics"] 配列にID追加
  - アイテム装備可能化
- **Behavior**:
  1. ショップUIでアイテムリスト表示（価格付き）
  2. 購入ボタン押下 → コイン残高チェック
  3. 不足時エラー、十分ならコイン減算 + 解禁フラグ追加
  4. 装備UIで即座に反映可能

#### Feature: キャンプ木材ポイント管理
- **Description**: インスタンス専用通貨で拠点装飾・Camp Rank向上
- **Inputs**:
  - 納品された板材数
  - キャンプ建築完了イベント
- **Outputs**:
  - Camp Wood Points（UdonSynced int）
  - Camp Rank寄与
- **Behavior**:
  1. 板材納品時にポイント加算（1枚 = 10ポイント）
  2. 一定ポイント消費でインスタンス限定装飾購入
  3. Camp XPとは別管理（装飾用 vs ランク用）

---

### Capability: 実績・ランキングシステム（Achievements & Leaderboards）

やり込み度の可視化と競争要素

#### Feature: 実績判定
- **Description**: 条件達成時にトロフィー・称号を解禁
- **Inputs**:
  - 累積統計データ（総伐採本数、運搬回数など）
  - 実績定義テーブル
- **Outputs**:
  - 実績解禁フラグ（PlayerData["Achievements"] 配列）
  - 報酬（称号、限定コスメ）
- **Behavior**:
  1. 行動ごとに累積カウンタ更新
  2. 実績条件チェック（例: 総伐採 >= 100）
  3. 条件満たした瞬間に解禁演出 + PlayerData更新
  4. トロフィーウォールに自動表示

#### Feature: 栄誉棚（トロフィーウォール）
- **Description**: プレイヤー別の実績トロフィーを壁に展示
- **Inputs**:
  - プレイヤーの解禁済み実績リスト
  - プレイヤーのインスタンス参加状態
- **Outputs**:
  - 3Dトロフィーモデル表示
  - プレート上のプレイヤー名・称号
- **Behavior**:
  1. プレイヤー参加時、専用プレート列を生成
  2. 解禁済み実績に対応するトロフィーモデルを配置
  3. 他プレイヤーが見て「すごい」と思える配置
  4. 退室時にプレート削除

#### Feature: デイリーランキング
- **Description**: インスタンス内の当日伐採本数・運搬距離のTOP3表示
- **Inputs**:
  - 全プレイヤーの行動ログ（UdonSynced配列）
- **Outputs**:
  - ランキングボード（TOP3のプレイヤー名 + 数値）
  - 上位者への光るオーラ演出
- **Behavior**:
  1. UdonSynced配列でプレイヤーID・伐採数を管理
  2. 定期的（30秒ごと）にソートしてTOP3を計算
  3. ランキングボードUIを更新
  4. インスタンスリセット時にクリア

---

### Capability: コミュニケーション支援（Social Features）

会話を促進するUI・演出

#### Feature: ステータスアイコン表示
- **Description**: 頭上に活動状態アイコンを表示
- **Inputs**:
  - プレイヤーの現在アクション（伐採中/運搬中/クラフト中）
- **Outputs**:
  - 3Dアイコン（斧/箱/ハンマー）
  - ワールドスペースCanvas
- **Behavior**:
  1. 行動開始時にアイコンをアクティブ化
  2. 頭上1.5mの位置に追従表示
  3. 行動終了時に非アクティブ化
  4. 他プレイヤーから見える（ボイスなしでも活動把握可）

#### Feature: 撮影スポット
- **Description**: 写真映えする演出エリア
- **Inputs**:
  - プレイヤーがステージに乗ったイベント
- **Outputs**:
  - ライトON、背景ボケ、実績表示
  - スクリーンショット補助UI
- **Behavior**:
  1. 丸太ステージのトリガーコライダーでプレイヤー検知
  2. ステージライト点灯、ポストエフェクト適用
  3. 背景ボードに「今日の伐採本数: X本」表示
  4. プレイヤーが離れたら演出解除

#### Feature: クエストボード
- **Description**: 木材納品クエストの提示と進捗表示
- **Inputs**:
  - クエストテーブル（木の種類、目標数）
  - プレイヤーの納品進捗
- **Outputs**:
  - クエスト一覧UI（3つまで同時表示）
  - 進捗バー
  - 達成報酬
- **Behavior**:
  1. ボード接近でUIパネル表示
  2. クエスト選択 → 個人目標として設定
  3. 納品時に進捗更新（PlayerData同期）
  4. 達成時に報酬付与（コイン + XP）

---

### Capability: 戦闘・防衛システム（Combat & Defense）

熊の襲撃から キャンプを守る緊張感のあるイベント

#### Feature: 熊襲撃イベント
- **Description**: 60分ごとに熊が出現し、キャンプを襲撃する
- **Inputs**:
  - ゲーム経過時間（60分タイマー）
  - Camp Rank（熊のレベル決定）
  - プレイヤー数（襲撃規模調整）
- **Outputs**:
  - 警告音・UI通知（30秒前）
  - 熊オブジェクトのスポーン（キャンプ外縁）
  - 襲撃成功/失敗判定（3分以内に撃退 or 撤退）
- **Behavior**:
  1. 60分タイマー完了 → 警告音「熊が近づいている！」
  2. 30秒のカウントダウン（準備時間）
  3. 熊スポーン（レベル = Camp Rank、HP = 100 * Rank）
  4. 熊の行動パターン：
     - プレイヤー発見 → 追跡・攻撃（ダメージ30/回）
     - 設備発見 → 破壊試行（作業台、ベンチなど）
     - 3分経過 or HP=0 → 撤退 or 倒れる
  5. 撃退成功 → 報酬付与（コイン+200、熊の毛皮）

#### Feature: 初期防衛（非戦闘）
- **Description**: 装備がない状態での生存戦術
- **Inputs**:
  - プレイヤーの位置
  - 熊との距離
  - 隠蔽可能オブジェクト（建物、茂み）
- **Outputs**:
  - 逃走成功フラグ（キャンプ外へ退避）
  - 隠蔽成功フラグ（熊の視界外）
  - 時間稼ぎボーナス（丸太投擲）
- **Behavior**:
  1. **逃走**: キャンプ外50m以上離れると熊が追跡停止
  2. **隠蔽**: 建物内 or 茂みコライダー内で5秒静止 → 熊が見失う
  3. **時間稼ぎ**: 丸太をPickup → 熊に投げる（ダメージ5、怯み1秒）
  4. 3分経過で熊自動撤退（プレイヤー無傷でも報酬なし）

#### Feature: 武器システム
- **Description**: クラフトした武器で熊を攻撃
- **Inputs**:
  - 装備中の武器種別（槍、弓、斧）
  - プレイヤーの攻撃入力（トリガー）
  - Combatスキルレベル
- **Outputs**:
  - 武器スイングアニメーション
  - 熊へのダメージ適用
  - CombatスキルXP加算
- **Behavior**:
  1. **木の槍**（Tier 1）:
     - 必要素材: 板材2枚 + 枝3本
     - ダメージ: 10（Combatレベル補正 +5%/Lv）
     - リーチ: 2m
  2. **鉄の槍**（Tier 2）:
     - 必要素材: 板材5枚 + 鉄鉱石3個 + 熊の毛皮1枚
     - ダメージ: 20
     - リーチ: 2.5m
  3. **鋼の槍**（Tier 3）:
     - 必要素材: 板材10枚 + 鋼インゴット5個 + 熊の毛皮3枚
     - ダメージ: 35
     - リーチ: 3m
  4. **弓矢**（遠距離、Tier 2以降）:
     - ダメージ: 15
     - 射程: 20m
     - 矢の消費（1本/発、クラフト可能）

#### Feature: 罠・バリケードシステム
- **Description**: 事前設置型の防衛設備
- **Inputs**:
  - 設置位置（predefined ポイント）
  - 必要素材
  - 熊の接触判定
- **Outputs**:
  - 罠発動（ダメージ + 移動速度低下）
  - バリケード耐久度減少
  - 罠再設置必要フラグ
- **Behavior**:
  1. **落とし穴**（Tier 1）:
     - 素材: 板材5枚 + 枝10本
     - 効果: 熊が踏むとダメージ30 + 3秒移動不可
     - 使い捨て（1回発動で消滅）
  2. **トゲ罠**（Tier 2）:
     - 素材: 板材10枚 + 鉄鉱石5個
     - 効果: ダメージ15/秒（範囲内）+ 移動速度-50%
     - 耐久度: HP50（熊の攻撃で破壊可能）
  3. **強化バリケード**（Tier 2）:
     - 素材: 丸太5本 + 板材20枚
     - 効果: 熊の進路を遮断
     - 耐久度: HP100
  4. **自動罠**（Tier 3）:
     - 素材: 板材30枚 + 鋼インゴット10個 + 歯車5個
     - 効果: 熊検知で自動発動（矢を発射、ダメージ20）
     - 弾数: 10発（補充可能）

#### Feature: プレイヤーHP・防具システム
- **Description**: 熊の攻撃を受けるとHPが減少、防具で軽減
- **Inputs**:
  - 熊の攻撃ヒット判定
  - 装備中の防具
  - Combatスキルレベル
- **Outputs**:
  - HP減少量（基礎30 - 防具軽減値）
  - ダメージエフェクト（画面赤フラッシュ）
  - HP=0でリスポーン処理
- **Behavior**:
  1. **HP管理**:
     - 初期HP: 100
     - 熊の攻撃: -30（防具なし）
     - HP自動回復: 1/秒（戦闘外）
  2. **防具Tier**:
     - **革の鎧**（Tier 1）: 熊の毛皮2枚、ダメージ軽減-10%
     - **鉄の鎧**（Tier 2）: 熊の毛皮5枚 + 鉄鉱石10個、軽減-30%
     - **鋼の鎧**（Tier 3）: 熊の毛皮10枚 + 鋼インゴット15個、軽減-50%
  3. **倒された時**:
     - 即座にキャンプ中央でリスポーン（ペナルティなし）
     - 装備は保持（ドロップなし）
     - 熊は他のプレイヤーを追跡継続

#### Feature: Combatスキル成長
- **Description**: 戦闘行動でCombatスキルが成長
- **Inputs**:
  - 熊へのダメージ量
  - 罠設置・発動
  - 熊撃退成功
- **Outputs**:
  - CombatスキルXP加算
  - レベルアップ時のパッシブ効果
  - 新レシピ解禁
- **Behavior**:
  1. XP獲得:
     - 熊にダメージ10 = Combat +5 XP
     - 罠発動 = +10 XP
     - 熊撃退成功 = +100 XP
  2. レベル効果:
     - Lv1〜5: 武器ダメージ +5%/Lv
     - Lv6〜10: ダメージ +10%/Lv、HP最大値 +10/Lv
     - Lv10以上: 罠設置数 +1（通常3個 → 4個）
  3. レベル6分岐:
     - **攻撃特化**: ダメージ +20%、クリティカル率10%
     - **防御特化**: 被ダメージ -20%、HP回復速度2倍

#### Feature: 熊の毛皮・特殊素材
- **Description**: 熊を倒すと入手できる希少素材
- **Inputs**:
  - 熊撃退成功イベント
  - 熊のレベル
- **Outputs**:
  - 熊の毛皮 × 1〜3枚（レベル依存）
  - きこりコイン +200〜500
  - 実績カウント加算
- **Behavior**:
  1. 熊のレベルで毛皮数決定:
     - Lv1〜3: 1枚
     - Lv4〜7: 2枚
     - Lv8〜10: 3枚
  2. 毛皮の用途:
     - 防具クラフト
     - Tier 2武器のアップグレード素材
     - 個人キャンプの装飾（熊の敷物）
  3. コイン報酬:
     - 基礎200コイン + (Lv × 30)
     - 無傷撃退ボーナス: +100コイン

---

### Capability: 個人キャンプ（拡張要素）（Personal Camp - Future）

プレイヤー専用の装飾スペース

#### Feature: 個人区画アクセス
- **Description**: ポータル経由で個人キャンプへ移動
- **Inputs**:
  - ポータルインタラクション
  - PlayerDataの区画ID
- **Outputs**:
  - シーン遷移（メインキャンプ ↔ 個人区画）
  - 個人区画の家具配置データ読み込み
- **Behavior**:
  1. ポータルトリガーでシーンロード開始
  2. PlayerData["PersonalCampData"] から配置情報取得
  3. 家具オブジェクトを指定座標・回転で Instantiate
  4. **v1では未実装** → v1.1以降の拡張要素

#### Feature: 家具配置・保存
- **Description**: 購入した家具を自由に配置し、PlayerDataに保存
- **Inputs**:
  - 家具オブジェクト
  - 配置位置・回転
- **Outputs**:
  - PlayerData["PersonalCampData"] JSON文字列
  - 配置済み家具のビジュアル
- **Behavior**:
  1. 家具ピックアップで配置モード開始
  2. グリッド吸着 or 自由配置
  3. 確定時に位置・家具IDを配列化
  4. JSON文字列化してPlayerDataに保存（100KB制限考慮）

---

## 🏗️ 構造分解（Structural Decomposition）

### Repository Structure（Unity Project）

```
Assets/
├── WoodcutterCamp/
│   ├── Scenes/
│   │   ├── MainCamp.unity               # メインキャンプシーン
│   │   └── PersonalCamp_Template.unity  # 個人キャンプテンプレート（v1.1）
│   ├── Udon/
│   │   ├── Core/                        # 基盤システム
│   │   │   ├── GameManager.cs          # ゲーム全体管理
│   │   │   ├── NetworkSync.cs          # ネットワーク同期基盤
│   │   │   └── PersistenceManager.cs   # PlayerData読み書き
│   │   ├── Woodcutting/                 # 伐採モジュール
│   │   │   ├── TreeController.cs       # 木の状態管理
│   │   │   ├── AxeInteraction.cs       # 斧との相互作用
│   │   │   └── LogSpawner.cs           # 丸太生成
│   │   ├── Carrying/                    # 運搬モジュール
│   │   │   ├── LogPickup.cs            # 小丸太Pickup
│   │   │   ├── HeavyLogSync.cs         # 太丸太協力運搬
│   │   │   └── CarryingTracker.cs      # 距離計測
│   │   ├── Processing/                  # 加工モジュール
│   │   │   ├── CuttingTable.cs         # 切断台
│   │   │   ├── Sawmill.cs              # 製材所
│   │   │   └── CraftingStation.cs      # クラフト台
│   │   ├── Progression/                 # 成長モジュール
│   │   │   ├── SkillManager.cs         # スキルXP・レベル管理
│   │   │   ├── SkillEffects.cs         # パッシブ効果適用
│   │   │   └── CampRankManager.cs      # Camp Rank管理
│   │   ├── Economy/                     # 経済モジュール
│   │   │   ├── CoinManager.cs          # きこりコイン管理
│   │   │   ├── ShopUI.cs               # コスメショップ
│   │   │   └── CampPointsManager.cs    # キャンプポイント管理
│   │   ├── Combat/                      # 戦闘・防衛モジュール
│   │   │   ├── BearAI.cs               # 熊の行動AI
│   │   │   ├── BearSpawner.cs          # 熊襲撃イベント管理
│   │   │   ├── PlayerHealth.cs         # プレイヤーHP管理
│   │   │   ├── WeaponController.cs     # 武器攻撃処理
│   │   │   ├── TrapSystem.cs           # 罠・バリケード管理
│   │   │   └── DefenseEquipment.cs     # 防具システム
│   │   ├── Social/                      # ソーシャルモジュール
│   │   │   ├── AchievementChecker.cs   # 実績判定
│   │   │   ├── TrophyWall.cs           # 栄誉棚表示
│   │   │   ├── Leaderboard.cs          # ランキング計算
│   │   │   └── StatusIcon.cs           # ステータスアイコン
│   │   └── UI/                          # UI統合モジュール
│   │       ├── HUDManager.cs           # HUD表示制御
│   │       ├── QuestBoard.cs           # クエストボード
│   │       └── PhotoSpot.cs            # 撮影スポット
│   ├── Prefabs/
│   │   ├── Trees/                       # 木プレハブ
│   │   ├── Logs/                        # 丸太プレハブ
│   │   ├── Equipment/                   # 設備プレハブ
│   │   └── Cosmetics/                   # コスメアイテム
│   ├── Materials/
│   ├── Textures/                        # Quest対応（1024x1024以下）
│   ├── Audio/
│   └── VFX/
└── Packages/
    └── com.vrchat.worlds/               # SDK 3.9.0
```

### Module Definitions

#### Module: Core（基盤システム）
- **Maps to capability**: 全システム共通の基盤機能
- **Responsibility**: ゲーム初期化、ネットワーク基盤、データ永続化の単一管理
- **File structure**:
  ```
  Core/
  ├── GameManager.cs          # シングルトン、初期化順序制御
  ├── NetworkSync.cs          # UdonSynced変数の抽象化レイヤー
  └── PersistenceManager.cs   # PlayerData CRUD操作の統一I/F
  ```
- **Exports**:
  - `GameManager.Instance` - グローバルアクセスポイント
  - `NetworkSync.SyncVariable<T>(key, value)` - 型安全な同期API
  - `PersistenceManager.SavePlayerData(key, value)` - PlayerData保存
  - `PersistenceManager.LoadPlayerData<T>(key)` - PlayerData読み込み

**設計原則（SOLID）**:
- **Single Responsibility**: GameManagerはゲーム起動のみ、データ保存はPersistenceManagerに委譲
- **Open/Closed**: NetworkSyncは継承可能な抽象クラス、各モジュールで拡張

---

#### Module: Woodcutting（伐採）
- **Maps to capability**: 伐採システム（Woodcutting）
- **Responsibility**: 木の状態管理、伐採判定、倒木処理の単一ドメイン
- **File structure**:
  ```
  Woodcutting/
  ├── TreeController.cs       # 木のHP・状態遷移
  ├── AxeInteraction.cs       # 斧の当たり判定・ダメージ計算
  └── LogSpawner.cs           # 丸太生成ロジック
  ```
- **Exports**:
  - `TreeController.TakeDamage(damage, playerID)` - ダメージ適用
  - `TreeController.OnTreeFelled` - 倒木イベント
  - `AxeInteraction.CalculateDamage(skillLevel)` - スキル補正付きダメージ
  - `LogSpawner.SpawnLogs(treeType, position)` - 丸太生成

**設計原則（SOLID）**:
- **Dependency Inversion**: TreeControllerはISkillManagerインターフェースに依存（具体実装に依存しない）
- **Interface Segregation**: AxeInteractionは攻撃判定のみ、移動や音は別モジュール

---

#### Module: Carrying（運搬）
- **Maps to capability**: 運搬システム（Carrying）
- **Responsibility**: 丸太Pickup、協力運搬、距離計測の単一ドメイン
- **File structure**:
  ```
  Carrying/
  ├── LogPickup.cs            # VRC_Pickupラッパー、速度デバフ
  ├── HeavyLogSync.cs         # 太丸太の複数人同期
  └── CarryingTracker.cs      # 距離計測とXP計算
  ```
- **Exports**:
  - `LogPickup.OnPickedUp` - Pickup開始イベント
  - `LogPickup.OnDropped` - Drop完了イベント
  - `HeavyLogSync.UpdatePosition(playerPositions)` - 中間位置計算
  - `CarryingTracker.RecordDistance(distance)` - スキルXP加算

**設計原則（SOLID）**:
- **Single Responsibility**: CarryingTrackerは計測のみ、XP付与はSkillManagerに委譲
- **Liskov Substitution**: LogPickupを継承したHeavyLogPickupでも同じインターフェース

---

#### Module: Processing（加工・クラフト）
- **Maps to capability**: 加工・クラフトシステム（Processing & Crafting）
- **Responsibility**: 資材変換、クラフトレシピ管理の単一ドメイン
- **File structure**:
  ```
  Processing/
  ├── CuttingTable.cs         # 基本加工設備
  ├── Sawmill.cs              # 高効率加工設備
  └── CraftingStation.cs      # 建築クラフト
  ```
- **Exports**:
  - `CuttingTable.ProcessLog(logType)` - 丸太→板材変換
  - `Sawmill.BatchProcess(logs[])` - 一括変換
  - `CraftingStation.CanCraft(recipeID, playerID)` - クラフト可否判定
  - `CraftingStation.Craft(recipeID, playerID)` - 実行＋素材消費

**設計原則（SOLID）**:
- **Open/Closed**: CraftingStationは新レシピ追加に開いているが、コード変更は閉じている（ScriptableObjectで管理）
- **Dependency Inversion**: レシピデータはIRecipeDataインターフェース経由で取得

---

#### Module: Progression（成長）
- **Maps to capability**: 成長システム（Progression）
- **Responsibility**: スキルXP/レベル/Camp Rankの単一管理
- **File structure**:
  ```
  Progression/
  ├── SkillManager.cs         # PlayerDataベースのスキル管理
  ├── SkillEffects.cs         # パッシブ効果計算
  └── CampRankManager.cs      # インスタンス共有Rank管理
  ```
- **Exports**:
  - `SkillManager.AddXP(skillType, amount)` - XP加算
  - `SkillManager.GetSkillLevel(skillType)` - 現在レベル取得
  - `SkillEffects.GetDamageMultiplier(level)` - 補正値計算
  - `CampRankManager.AddCampXP(amount)` - Camp XP加算

**設計原則（SOLID）**:
- **Single Responsibility**: SkillManagerはデータ管理のみ、効果計算はSkillEffectsに委譲
- **Open/Closed**: 新スキル追加はenum拡張のみ、既存コード変更不要

---

#### Module: Economy（経済）
- **Maps to capability**: 通貨・交換システム（Economy）
- **Responsibility**: コイン管理、ショップ購入、ポイント管理の単一ドメイン
- **File structure**:
  ```
  Economy/
  ├── CoinManager.cs          # きこりコイン増減
  ├── ShopUI.cs               # コスメ購入UI
  └── CampPointsManager.cs    # キャンプポイント管理
  ```
- **Exports**:
  - `CoinManager.AddCoins(amount)` - コイン付与
  - `CoinManager.SpendCoins(amount)` - コイン消費（残高チェック付き）
  - `ShopUI.PurchaseItem(itemID)` - 購入処理
  - `CampPointsManager.AddPoints(amount)` - インスタンスポイント加算

**設計原則（SOLID）**:
- **Dependency Inversion**: ShopUIはICoinManagerインターフェースに依存
- **Interface Segregation**: 購入処理はIPurchasableインターフェースで統一

---

#### Module: Combat（戦闘・防衛）
- **Maps to capability**: 戦闘・防衛システム（Combat & Defense）
- **Responsibility**: 熊AI、襲撃イベント、プレイヤーHP、武器・罠管理の単一ドメイン
- **File structure**:
  ```
  Combat/
  ├── BearAI.cs               # 熊の行動AI・攻撃判定
  ├── BearSpawner.cs          # 60分タイマー・襲撃管理
  ├── PlayerHealth.cs         # HP管理・ダメージ処理
  ├── WeaponController.cs     # 武器スイング・ダメージ計算
  ├── TrapSystem.cs           # 罠設置・発動処理
  └── DefenseEquipment.cs     # 防具装備・軽減計算
  ```
- **Exports**:
  - `BearAI.TakeDamage(damage)` - 熊へのダメージ適用
  - `BearAI.OnBearDefeated` - 熊撃退イベント
  - `BearSpawner.StartRaidEvent()` - 襲撃開始
  - `PlayerHealth.ApplyDamage(damage, source)` - プレイヤーダメージ処理
  - `WeaponController.Attack(target)` - 武器攻撃
  - `TrapSystem.PlaceTrap(trapType, position)` - 罠設置
  - `DefenseEquipment.GetDamageReduction()` - 防具軽減率取得

**設計原則（SOLID）**:
- **Single Responsibility**: BearAIは熊の行動のみ、スポーン管理はBearSpawnerに委譲
- **Dependency Inversion**: WeaponControllerはISkillManagerインターフェースに依存（Combat補正）
- **Open/Closed**: 新武器・罠追加はScriptableObjectで管理、コード変更不要
- **Interface Segregation**: PlayerHealthはダメージ処理のみ、UI表示はHUDManagerに委譲

---

#### Module: Social（ソーシャル）
- **Maps to capability**: 実績・ランキングシステム（Achievements & Leaderboards）
- **Responsibility**: 実績判定、ランキング計算、栄誉棚表示の単一ドメイン
- **File structure**:
  ```
  Social/
  ├── AchievementChecker.cs   # 実績条件判定
  ├── TrophyWall.cs           # 栄誉棚生成
  ├── Leaderboard.cs          # ランキングソート
  └── StatusIcon.cs           # ステータスアイコン
  ```
- **Exports**:
  - `AchievementChecker.CheckAchievements(playerID)` - 実績判定
  - `TrophyWall.UpdateTrophies(playerID)` - トロフィー更新
  - `Leaderboard.GetTopPlayers(category, count)` - TOP N取得
  - `StatusIcon.SetStatus(status)` - アイコン変更

**設計原則（SOLID）**:
- **Single Responsibility**: AchievementCheckerは判定のみ、報酬付与はCoinManagerに委譲
- **Open/Closed**: 新実績はScriptableObjectで追加、コード変更不要

---

#### Module: UI（UIシステム）
- **Maps to capability**: コミュニケーション支援（Social Features）の一部
- **Responsibility**: HUD、クエストボード、撮影スポットのUI表示単一ドメイン
- **File structure**:
  ```
  UI/
  ├── HUDManager.cs           # スキルXPバー、コイン表示
  ├── QuestBoard.cs           # クエスト一覧UI
  └── PhotoSpot.cs            # 撮影演出制御
  ```
- **Exports**:
  - `HUDManager.ShowXPGain(skillType, amount)` - XP獲得演出
  - `QuestBoard.ShowQuests(questList)` - クエスト表示
  - `PhotoSpot.ActivateEffect()` - ライト・背景ボケON

**設計原則（SOLID）**:
- **Single Responsibility**: HUDManagerは表示のみ、データ取得はSkillManagerに依存
- **Dependency Inversion**: UIはIDataProviderインターフェース経由でデータ取得

---

## 🔗 依存グラフ（Dependency Graph）

### Foundation Layer (Phase 0) - 依存なし

これらは他のモジュールに依存しない基盤

- **Core/GameManager**: ゲーム起動・初期化制御
- **Core/NetworkSync**: UdonSynced変数の抽象化レイヤー
- **Core/PersistenceManager**: PlayerData読み書きの統一インターフェース

**提供機能**:
- シングルトンアクセス
- 型安全な同期API
- データ永続化API（100KB制限考慮）

---

### Data Layer (Phase 1)

- **Progression/SkillManager**: 依存 → [PersistenceManager, NetworkSync]
  - PlayerDataからスキルレベル読み込み、XP加算時に保存
  - ネットワーク同期不要（個人データ）

- **Progression/SkillEffects**: 依存 → [SkillManager]
  - スキルレベルから補正値計算（Pure Function的）

- **Progression/CampRankManager**: 依存 → [NetworkSync]
  - インスタンス共有のCamp Rank管理
  - PlayerData非依存（インスタンスリセット仕様）

- **Economy/CoinManager**: 依存 → [PersistenceManager, NetworkSync]
  - きこりコインの増減・保存
  - ネットワーク同期不要（個人データ）

---

### Core Logic Layer (Phase 2)

- **Woodcutting/TreeController**: 依存 → [NetworkSync, SkillEffects, CampRankManager]
  - 木のHP同期
  - スキルレベルからダメージ補正取得
  - Camp Rankでリスポーン速度調整

- **Woodcutting/AxeInteraction**: 依存 → [TreeController, SkillManager]
  - TreeControllerにダメージ通知
  - 伐採完了時にSkillManagerへXP加算

- **Woodcutting/LogSpawner**: 依存 → [TreeController]
  - TreeControllerの倒木イベントを監視
  - 丸太生成（スキル依存なし）

- **Carrying/LogPickup**: 依存 → [SkillEffects]
  - Carryingレベルから移動速度デバフ計算

- **Carrying/HeavyLogSync**: 依存 → [NetworkSync, LogPickup]
  - 複数プレイヤーの位置同期
  - LogPickupの拡張（継承 or コンポジション）

- **Carrying/CarryingTracker**: 依存 → [LogPickup, SkillManager]
  - Pickup/Dropイベントを監視
  - 距離計算後、SkillManagerへXP加算

- **Processing/CuttingTable**: 依存 → [SkillEffects]
  - Craftingレベルで副産物確率調整

- **Processing/Sawmill**: 依存 → [CuttingTable, CampRankManager]
  - CuttingTableの拡張（高効率版）
  - Camp Rank 5以上で解禁判定

- **Processing/CraftingStation**: 依存 → [CoinManager, CampRankManager, SkillManager]
  - 素材消費時にCoinManager経由でコイン確認
  - Camp XP加算
  - クラフト完了時にSkillManager.AddXP

---

### Combat Layer (Phase 2.5)

- **Combat/BearSpawner**: 依存 → [NetworkSync, CampRankManager, GameManager]
  - 60分タイマー管理（GameManagerの経過時間参照）
  - Camp Rankから熊レベル決定
  - 襲撃イベントをUdonSyncedで全プレイヤーに同期

- **Combat/BearAI**: 依存 → [NetworkSync, PlayerHealth]
  - 熊のHP・状態をUdonSyncedで同期
  - プレイヤー発見時にPlayerHealth.ApplyDamage呼び出し
  - 設備破壊判定（CraftingStationなどの耐久度減少）

- **Combat/PlayerHealth**: 依存 → [NetworkSync, DefenseEquipment]
  - プレイヤーHPをローカル管理（同期不要）
  - DefenseEquipment.GetDamageReduction()で防具軽減計算
  - HP=0でリスポーン処理

- **Combat/WeaponController**: 依存 → [BearAI, SkillManager]
  - 武器スイング → BearAI.TakeDamage呼び出し
  - 熊撃退成功時にSkillManager.AddXP（Combat）呼び出し
  - SkillManagerからダメージ補正値取得

- **Combat/TrapSystem**: 依存 → [CraftingStation, BearAI, NetworkSync]
  - CraftingStationで罠をクラフト
  - 罠発動時にBearAI.TakeDamage呼び出し
  - 罠の設置状態をUdonSyncedで同期

- **Combat/DefenseEquipment**: 依存 → [SkillManager, CraftingStation]
  - SkillManagerからCombatレベル取得（防具強化）
  - CraftingStationで防具をクラフト
  - 軽減率計算（Pure Function的）

---

### Feature Layer (Phase 3)

- **Social/AchievementChecker**: 依存 → [PersistenceManager, SkillManager, CoinManager]
  - PlayerDataから累積統計取得
  - 実績解禁時にコイン報酬付与

- **Social/TrophyWall**: 依存 → [AchievementChecker, PersistenceManager]
  - 解禁済み実績リスト取得
  - プレートとトロフィーの動的生成

- **Social/Leaderboard**: 依存 → [NetworkSync]
  - 全プレイヤーの統計をUdonSynced配列で共有
  - ソート処理（軽量化のため30秒ごと）

- **Social/StatusIcon**: 依存 → [TreeController, LogPickup, CraftingStation]
  - 各モジュールのイベントを監視
  - アイコン表示切り替え

- **Economy/ShopUI**: 依存 → [CoinManager, PersistenceManager]
  - コイン残高表示
  - 購入時にCoinManager.SpendCoins呼び出し

- **Economy/CampPointsManager**: 依存 → [NetworkSync, CampRankManager]
  - Camp Pointsをインスタンス共有
  - Camp Rank寄与計算

- **UI/HUDManager**: 依存 → [SkillManager, CoinManager]
  - スキルXPバー、コイン表示
  - 各マネージャーから定期的にデータ取得

- **UI/QuestBoard**: 依存 → [PersistenceManager, CoinManager, SkillManager]
  - クエスト進捗をPlayerDataから取得
  - 達成時に報酬付与

- **UI/PhotoSpot**: 依存 → [Leaderboard, CampRankManager]
  - 背景ボードに今日の実績表示
  - ライト演出（他モジュール非依存）

---

### Dependency Chain Summary

```
Phase 0 (Foundation):
  Core/GameManager
  Core/NetworkSync
  Core/PersistenceManager

Phase 1 (Data):
  Progression/SkillManager → [PersistenceManager, NetworkSync]
  Progression/SkillEffects → [SkillManager]
  Progression/CampRankManager → [NetworkSync]
  Economy/CoinManager → [PersistenceManager, NetworkSync]

Phase 2 (Core Logic):
  Woodcutting/TreeController → [NetworkSync, SkillEffects, CampRankManager]
  Woodcutting/AxeInteraction → [TreeController, SkillManager]
  Woodcutting/LogSpawner → [TreeController]
  Carrying/LogPickup → [SkillEffects]
  Carrying/HeavyLogSync → [NetworkSync, LogPickup]
  Carrying/CarryingTracker → [LogPickup, SkillManager]
  Processing/CuttingTable → [SkillEffects]
  Processing/Sawmill → [CuttingTable, CampRankManager]
  Processing/CraftingStation → [CoinManager, CampRankManager, SkillManager]

Phase 2.5 (Combat):
  Combat/BearSpawner → [NetworkSync, CampRankManager, GameManager]
  Combat/BearAI → [NetworkSync, PlayerHealth]
  Combat/PlayerHealth → [NetworkSync, DefenseEquipment]
  Combat/WeaponController → [BearAI, SkillManager]
  Combat/TrapSystem → [CraftingStation, BearAI, NetworkSync]
  Combat/DefenseEquipment → [SkillManager, CraftingStation]

Phase 3 (Features):
  Social/AchievementChecker → [PersistenceManager, SkillManager, CoinManager]
  Social/TrophyWall → [AchievementChecker, PersistenceManager]
  Social/Leaderboard → [NetworkSync]
  Social/StatusIcon → [TreeController, LogPickup, CraftingStation]
  Economy/ShopUI → [CoinManager, PersistenceManager]
  Economy/CampPointsManager → [NetworkSync, CampRankManager]
  UI/HUDManager → [SkillManager, CoinManager]
  UI/QuestBoard → [PersistenceManager, CoinManager, SkillManager]
  UI/PhotoSpot → [Leaderboard, CampRankManager]
```

---

## 🛤️ 実装ロードマップ（Implementation Roadmap）

### Phase 0: Foundation（基盤構築）

**Goal**: 他モジュールが依存する基盤システムの確立

**Entry Criteria**:
- Unityプロジェクト作成（Unity 2022.3.22f1）
- VRChat SDK 3.9.0インポート完了
- UdonSharp 1.1.9セットアップ完了

**Tasks**:

- [ ] **GameManager実装** (depends on: none)
  - Acceptance criteria:
    - シーン起動時に自動初期化
    - シングルトンパターンで他スクリプトからアクセス可能
    - 初期化順序制御（NetworkSync → PersistenceManager → 他モジュール）
  - Test strategy:
    - 単体: シングルトンの重複生成防止テスト
    - 統合: 複数モジュールからのアクセステスト

- [ ] **NetworkSync実装** (depends on: none)
  - Acceptance criteria:
    - UdonSynced変数を型安全にラップ
    - SyncVariable<T>(key, value) メソッドでint/float/string対応
    - Manual Sync / Continuous Sync切り替え可能
  - Test strategy:
    - 単体: 型変換の正確性テスト
    - 統合: 2クライアント間の同期テスト（Unity Editor + Build & Test）

- [ ] **PersistenceManager実装** (depends on: none)
  - Acceptance criteria:
    - SavePlayerData(key, value) / LoadPlayerData<T>(key) API
    - 容量監視（GetPlayerDataStorageUsage API利用）
    - 100KB超過時に警告ログ出力
  - Test strategy:
    - 単体: データ保存・読み込みの往復テスト
    - 統合: 複数キー保存後の容量計算テスト
    - エッジケース: 99KB保存後に1KB追加で警告テスト

**Exit Criteria**:
- 3つのスクリプトが正常にコンパイル
- Unityエディタでシーン再生時にエラーなし
- VRChat Build & Testでアップロード成功

**Delivers**:
- 他モジュール開発のための安定した基盤
- PlayerDataの読み書きが可能
- ネットワーク同期のテストが可能

---

### Phase 1: Data & Progression（データ・成長システム）

**Goal**: スキル・通貨の管理基盤を確立し、成長の手応えを実装

**Entry Criteria**: Phase 0 完了

**Tasks**:

- [ ] **SkillManager実装** (depends on: [PersistenceManager, NetworkSync])
  - Acceptance criteria:
    - Woodcutting/Carrying/Crafting スキルのXP・レベル管理
    - AddXP(skillType, amount) でXP加算 → PlayerData自動保存
    - GetSkillLevel(skillType) で現在レベル取得
    - レベルアップ時のイベント発火
  - Test strategy:
    - 単体: XP加算ロジック（レベル1→2の境界値テスト）
    - 単体: レベル計算式の正確性（XP = Lv * 100 + Lv^2 * 10）
    - 統合: PlayerDataへの保存・読み込みテスト

- [ ] **SkillEffects実装** (depends on: [SkillManager])
  - Acceptance criteria:
    - GetDamageMultiplier(level) でWoodcuttingレベルから補正値計算
    - GetSpeedDebuff(level) でCarryingレベルからデバフ軽減計算
    - GetCraftingBonus(level) で副産物確率計算
  - Test strategy:
    - 単体: 各計算式のテスト（Lv1: 1.05倍, Lv10: 1.5倍など）
    - エッジケース: Lv0、Lv100の補正値

- [ ] **CampRankManager実装** (depends on: [NetworkSync])
  - Acceptance criteria:
    - AddCampXP(amount) でインスタンス共有XP加算
    - UdonSyncedでCamp Rank全員に同期
    - Rank Up時のイベント発火（エリア解放、レシピ解放）
  - Test strategy:
    - 単体: Rank Up境界値テスト（Rank1→2）
    - 統合: 複数プレイヤーからのXP加算で正しく同期

- [ ] **CoinManager実装** (depends on: [PersistenceManager, NetworkSync])
  - Acceptance criteria:
    - AddCoins(amount) / SpendCoins(amount) API
    - 残高不足時はfalse返却（エラーハンドリング）
    - PlayerDataに保存
  - Test strategy:
    - 単体: 残高マイナス防止テスト
    - 統合: コイン獲得→保存→再ログイン→復元テスト

**Exit Criteria**:
- スキルXP加算で正しくレベルアップ
- レベル補正値が他モジュールで利用可能
- Camp Rankが複数プレイヤーで同期
- コイン増減がPlayerDataに永続化

**Delivers**:
- 成長の手応えを感じられる基盤
- 他モジュールがスキル補正を利用可能
- 通貨システムの基本動作

---

### Phase 2: Core Gameplay（コアゲームループ）

**Goal**: 伐採→運搬→加工の気持ちいいサイクルを実装

**Entry Criteria**: Phase 1 完了

**Tasks**:

- [ ] **TreeController実装** (depends on: [NetworkSync, SkillEffects, CampRankManager])
  - Acceptance criteria:
    - 木のHP管理（UdonSyncedで同期）
    - TakeDamage(damage, playerID) でHP減少
    - HP=0で倒木アニメーション（最後の一撃方向に倒れる）
    - リスポーンタイマー（Camp Rank補正）
  - Test strategy:
    - 単体: HP減少ロジック、HP=0検知
    - 統合: 2プレイヤー同時攻撃で協力ボーナス発動
    - パフォーマンス: 100本の木を同時に伐採してもFPS低下なし（Quest基準60fps維持）

- [ ] **AxeInteraction実装** (depends on: [TreeController, SkillManager])
  - Acceptance criteria:
    - トリガー入力で斧スイング
    - コライダー衝突でTreeController.TakeDamage呼び出し
    - 伐採完了時にSkillManager.AddXP呼び出し
    - 伐採音・パーティクル演出
  - Test strategy:
    - 単体: ダメージ計算（基礎10 + スキル補正）
    - 統合: 木を10回叩いてHP=0→倒木確認

- [ ] **LogSpawner実装** (depends on: [TreeController])
  - Acceptance criteria:
    - TreeController.OnTreeFelledイベント監視
    - 木の種類に応じて小丸太2〜4本生成
    - スキルレベルで丸太数+1ボーナス（Lv10以上）
  - Test strategy:
    - 単体: 木種別の丸太数テスト
    - 統合: 倒木→丸太生成→Pickup可能確認

- [ ] **LogPickup実装** (depends on: [SkillEffects])
  - Acceptance criteria:
    - VRC_Pickupで掴める
    - 保持中に移動速度デバフ適用（0.85倍）
    - Carryingレベルでデバフ軽減（Lv10で0.90倍）
  - Test strategy:
    - 単体: デバフ計算の正確性
    - 統合: Pickup→移動速度変化→Drop→速度復元

- [ ] **HeavyLogSync実装** (depends on: [NetworkSync, LogPickup])
  - Acceptance criteria:
    - 2つ以上のハンドルポイント配置
    - 2人同時Pickupで中間位置に丸太移動
    - 1人が離すと移動不可（重量判定）
  - Test strategy:
    - 統合: 2プレイヤーで協力運搬テスト（Build & Test必須）
    - エッジケース: 1人目Pickup→2人目Pickup→1人目Drop時の挙動

- [ ] **CarryingTracker実装** (depends on: [LogPickup, SkillManager])
  - Acceptance criteria:
    - Pickup時に開始位置保存
    - Drop時に距離計算
    - 距離に応じたXP付与（10m = 5XP）
  - Test strategy:
    - 単体: 距離計算の正確性（Vector3.Distance）
    - 統合: 運搬→XP加算→レベルアップ確認

- [ ] **CuttingTable実装** (depends on: [SkillEffects])
  - Acceptance criteria:
    - 丸太を載せてボタン押下
    - 3秒のプログレスバー表示
    - 丸太→板材2枚 + 枝1本に変換
    - Craftingレベルで副産物+1（Lv10以上）
  - Test strategy:
    - 単体: 変換レシピの正確性
    - 統合: 丸太配置→加工→板材生成確認

- [ ] **Sawmill実装** (depends on: [CuttingTable, CampRankManager])
  - Acceptance criteria:
    - Camp Rank 5以上で解禁
    - 太丸太5本を一括処理（10秒）
    - 板材15枚生成（通常の1.5倍効率）
  - Test strategy:
    - 単体: Rank解禁判定
    - 統合: 大量加工→板材生成確認

- [ ] **CraftingStation実装** (depends on: [CoinManager, CampRankManager, SkillManager])
  - Acceptance criteria:
    - 建築ポイントで板材消費
    - 素材不足時にエラー表示
    - 建築完了でCamp XP加算
    - クラフト完了でSkillManager.AddXP呼び出し
  - Test strategy:
    - 単体: 素材チェックロジック
    - 統合: 板材10枚消費→ベンチ建築→Camp Rank上昇

**Exit Criteria**:
- 木を伐採→丸太運搬→加工→クラフトの一連の流れが動作
- スキルXPが正しく加算される
- Camp Rankが上昇し、Sawmillが解禁される
- Quest環境で60fps維持（Udon最適化：Update削減、キャッシング活用）

**Delivers**:
- プレイ可能なコアゲームループ
- 20分の滞在で飽きないサイクル
- 協力作業の気持ちよさ

---

### Phase 2.5: Combat & Defense（戦闘・防衛システム）

**Goal**: 熊襲撃イベントで緊張感を追加し、協力防衛の楽しさを実装

**Entry Criteria**: Phase 2 完了

**Tasks**:

- [ ] **BearSpawner実装** (depends on: [NetworkSync, CampRankManager, GameManager])
  - Acceptance criteria:
    - 60分タイマー管理（ゲーム開始から経過時間計測）
    - Camp Rankから熊レベル決定（Rank = レベル）
    - 警告音・UI通知（30秒前）
    - 熊スポーン位置ランダム（キャンプ外縁5箇所から選択）
    - 襲撃イベントをUdonSyncedで全プレイヤーに同期
  - Test strategy:
    - 単体: タイマー60分経過テスト（デバッグ時は1分に短縮）
    - 統合: Camp Rank 3で熊レベル3スポーン確認
    - ネットワーク: 複数プレイヤーで同時に警告音受信

- [ ] **BearAI実装** (depends on: [NetworkSync, PlayerHealth])
  - Acceptance criteria:
    - 熊のHP・状態をUdonSyncedで同期
    - プレイヤー検知（視界20m、視野角120度）
    - 追跡・攻撃（ダメージ30、攻撃間隔2秒）
    - 設備破壊（作業台、ベンチなどを攻撃）
    - 3分経過 or HP=0で撤退・倒れる
  - Test strategy:
    - 単体: プレイヤー検知判定（距離、視野角）
    - 単体: HP減少→0で倒れる演出
    - 統合: プレイヤー攻撃→PlayerHealth.ApplyDamage呼び出し確認
    - パフォーマンス: 熊AI実行時にFPS低下なし（Quest60fps維持）

- [ ] **PlayerHealth実装** (depends on: [NetworkSync, DefenseEquipment])
  - Acceptance criteria:
    - プレイヤーHP管理（初期100、最大100 + Combatレベル*10）
    - 熊の攻撃でHP減少（基礎30 - 防具軽減）
    - HP自動回復（1/秒、戦闘外のみ）
    - HP=0でリスポーン（キャンプ中央、ペナルティなし）
  - Test strategy:
    - 単体: HP減少・回復ロジック
    - 単体: 防具軽減計算（30ダメージ - 30%軽減 = 21ダメージ）
    - 統合: 熊攻撃3回→HP=10→回復開始確認

- [ ] **WeaponController実装** (depends on: [BearAI, SkillManager])
  - Acceptance criteria:
    - 武器スイングアニメーション（槍、弓）
    - 当たり判定（コライダー衝突）
    - BearAI.TakeDamage呼び出し
    - Combatレベルでダメージ補正（基礎10 + Lv*5%）
    - 熊撃退成功時にSkillManager.AddXP（Combat）呼び出し
  - Test strategy:
    - 単体: ダメージ計算（木の槍10 + Combatレベル5 → 12.5ダメージ）
    - 統合: 槍10回スイング→熊撃退→Combat +100 XP確認

- [ ] **TrapSystem実装** (depends on: [CraftingStation, BearAI, NetworkSync])
  - Acceptance criteria:
    - 罠クラフト（CraftingStationで素材消費）
    - 罠設置（predefined ポイント、最大3個）
    - 罠発動（熊が踏むとダメージ + 移動速度低下）
    - 罠状態をUdonSyncedで同期
  - Test strategy:
    - 単体: 罠発動判定（コライダー侵入）
    - 統合: 落とし穴設置→熊踏む→ダメージ30 + 3秒移動不可確認
    - ネットワーク: 罠発動が全プレイヤーで同期

- [ ] **DefenseEquipment実装** (depends on: [SkillManager, CraftingStation])
  - Acceptance criteria:
    - 防具装備UI（革・鉄・鋼の鎧選択）
    - 軽減率計算（革10%、鉄30%、鋼50%）
    - Combatレベルで追加軽減（+1%/Lv）
    - 装備情報をPlayerDataに保存
  - Test strategy:
    - 単体: 軽減率計算（鉄の鎧30% + Combatレベル5 → 35%軽減）
    - 統合: 防具装備→熊攻撃→ダメージ軽減確認

- [ ] **Combatスキル統合** (depends on: [SkillManager, WeaponController, TrapSystem])
  - Acceptance criteria:
    - SkillManagerにCombatスキル追加（Woodcutting/Carryingと同様）
    - XP獲得: ダメージ10 = +5 XP、熊撃退 = +100 XP
    - レベル効果: ダメージ +5%/Lv、HP最大値 +10/Lv
    - レベル6分岐: 攻撃特化 or 防御特化
  - Test strategy:
    - 単体: XP加算ロジック
    - 統合: 熊撃退3回→Combat Lv2昇格確認

**Exit Criteria**:
- 60分タイマーで熊襲撃イベント発動
- 初期状態（装備なし）で逃走・隠蔽が可能
- 木の槍クラフト→熊攻撃→撃退成功の流れが動作
- 罠設置→熊踏む→ダメージ発動が動作
- 熊撃退で熊の毛皮・コイン獲得
- Combat スキルが正しく成長
- Quest環境で60fps維持（熊AIの最適化）

**Delivers**:
- まったり作業に緊張感を追加
- 協力防衛の楽しさ
- やり込み要素（武器・防具のアップグレード）
- 特殊素材（熊の毛皮）の経済循環

---

### Phase 3: Social & Retention（ソーシャル・継続要素）

**Goal**: 実績・ランキング・コスメで「見せびらかし」と継続プレイを促進

**Entry Criteria**: Phase 2 完了

**Tasks**:

- [ ] **AchievementChecker実装** (depends on: [PersistenceManager, SkillManager, CoinManager])
  - Acceptance criteria:
    - 累積統計（総伐採本数など）をPlayerDataから取得
    - 実績条件判定（木100本伐採など）
    - 解禁時にCoinManager.AddCoins呼び出し
    - PlayerData["Achievements"]配列更新
  - Test strategy:
    - 単体: 実績条件のロジック（境界値テスト）
    - 統合: 木100本伐採→実績解禁→コイン付与確認

- [ ] **TrophyWall実装** (depends on: [AchievementChecker, PersistenceManager])
  - Acceptance criteria:
    - プレイヤー参加時に専用プレート生成
    - 解禁済み実績のトロフィーモデル配置
    - 退室時にプレート削除（メモリリーク防止）
  - Test strategy:
    - 統合: 実績3つ解禁→トロフィー3つ表示確認
    - パフォーマンス: 10プレイヤー同時参加でトロフィー表示（Quest60fps維持）

- [ ] **Leaderboard実装** (depends on: [NetworkSync])
  - Acceptance criteria:
    - 全プレイヤーの伐採本数をUdonSynced配列で共有
    - 30秒ごとにソート（Update削減のためタイマー使用）
    - TOP3をランキングボードに表示
  - Test strategy:
    - 単体: ソートアルゴリズム（バブルソート、配列長<20想定）
    - 統合: 5プレイヤーで伐採→ランキング更新確認

- [ ] **StatusIcon実装** (depends on: [TreeController, LogPickup, CraftingStation])
  - Acceptance criteria:
    - 伐採中に斧アイコン表示
    - 運搬中に箱アイコン表示
    - クラフト中にハンマーアイコン表示
    - 頭上1.5mに追従（ワールドスペースCanvas）
  - Test strategy:
    - 統合: 伐採開始→アイコン表示→伐採終了→アイコン非表示

- [ ] **ShopUI実装** (depends on: [CoinManager, PersistenceManager])
  - Acceptance criteria:
    - コスメアイテムリスト表示（斧スキン、帽子など）
    - 購入ボタン押下→CoinManager.SpendCoins呼び出し
    - 残高不足時にエラー表示
    - 購入済みアイテムをPlayerData["UnlockedCosmetics"]に保存
  - Test strategy:
    - 単体: 残高不足時のエラーハンドリング
    - 統合: コイン100枚獲得→斧スキン購入→装備確認

- [ ] **CampPointsManager実装** (depends on: [NetworkSync, CampRankManager])
  - Acceptance criteria:
    - 板材納品でポイント加算（UdonSynced）
    - インスタンス限定装飾の購入に使用
  - Test strategy:
    - 統合: 板材10枚納品→100ポイント加算→装飾購入

- [ ] **HUDManager実装** (depends on: [SkillManager, CoinManager])
  - Acceptance criteria:
    - スキルXPバー（アクション時のみフェード表示）
    - コイン残高表示（画面左下）
    - レベルアップ演出（パーティクル + SE）
  - Test strategy:
    - 統合: 木伐採→XPバー表示→フェードアウト確認

- [ ] **QuestBoard実装** (depends on: [PersistenceManager, CoinManager, SkillManager])
  - Acceptance criteria:
    - クエスト一覧表示（3つまで同時）
    - 進捗バー表示（PlayerDataから取得）
    - 達成時に報酬付与（コイン + XP）
  - Test strategy:
    - 統合: クエスト「木10本伐採」→進捗10/10→報酬付与確認

- [ ] **PhotoSpot実装** (depends on: [Leaderboard, CampRankManager])
  - Acceptance criteria:
    - ステージに乗るとライトON
    - 背景ボードに「今日の伐採本数: X本」表示
    - ポストエフェクト（背景ボケ）適用
  - Test strategy:
    - 統合: ステージ乗る→演出ON→降りる→演出OFF

**Exit Criteria**:
- 実績解禁→トロフィー表示→コイン報酬の流れが動作
- ランキングTOP3が正しく表示される
- コスメ購入→装備が正常に動作
- クエスト達成→報酬付与が動作
- HUDが見やすく、邪魔にならない

**Delivers**:
- 継続プレイのモチベーション（実績・コレクション）
- 見せびらかし要素（トロフィー、ランキング）
- 自然な会話促進（ステータスアイコン、撮影スポット）

---

### Phase 4: Polish & Optimization（磨き・最適化）

**Goal**: Quest対応、パフォーマンス最適化、UX改善

**Entry Criteria**: Phase 3 完了

**Tasks**:

- [ ] **Quest最適化** (depends on: [全Phase])
  - Acceptance criteria:
    - ファイルサイズ < 100MB（圧縮後）
    - ポリゴン数 < 15,000（Medium維持）
    - テクスチャ解像度 1024x1024以下
    - マテリアル数 最小化（アトラスパッキング）
    - Quest Build & Testで60fps維持
  - Test strategy:
    - パフォーマンス: Quest 2実機で20分プレイ
    - ファイルサイズ: Unity Build Report確認

- [ ] **Udon最適化** (depends on: [全Phase])
  - Acceptance criteria:
    - Updateループ削減（タイマーベースに変更）
    - GetComponent呼び出しをStart()でキャッシング
    - 重いループのタイムスライシング（100要素を10個ずつ10フレーム分散）
    - ネットワーク帯域 < 10KB/秒
  - Test strategy:
    - パフォーマンス: VRChat Profilerでフレーム時間計測
    - ネットワーク: SDK Network Debugで帯域確認

- [ ] **チュートリアル実装** (depends on: [UI/HUDManager])
  - Acceptance criteria:
    - 初回訪問時のみ表示（PlayerDataフラグ管理）
    - 案内看板（斧を取る→木を叩く→運ぶ→加工）
    - ガイド矢印（控えめ演出）
  - Test strategy:
    - 統合: 新規プレイヤーでテスト→チュートリアル表示→2回目非表示確認

- [ ] **サウンドデザイン** (depends on: [Woodcutting, Carrying, Processing])
  - Acceptance criteria:
    - 伐採音（気持ちいいが耳障りにならない音量）
    - 倒木音（Valheim風の重厚感）
    - 丸太運搬音（足音の変化）
    - キャンプBGM（控えめ、会話重視）
  - Test strategy:
    - UX: 5人でテストプレイ→会話しやすさ確認

- [ ] **マップ最終調整** (depends on: [全Phase])
  - Acceptance criteria:
    - 手前の森（初級）、奥の森（中級）のビジュアル差別化
    - Camp Rank 5で奥の森解禁（コライダーで封鎖→解除）
    - Lightmap焼き（Quest対応）
    - LOD設定（距離に応じてポリゴン削減）
  - Test strategy:
    - ビジュアル: PC/Questで見た目確認
    - パフォーマンス: 奥の森で60fps維持

**Exit Criteria**:
- Quest 2実機で60fps安定
- ファイルサイズ < 100MB
- 初回プレイヤーがチュートリアルで操作理解
- 音が気持ちよく、会話を邪魔しない
- VRChat公式ガイドラインに準拠（最適化チェックリスト全項目クリア）

**Delivers**:
- Quest対応完了（PCとQuestでリリース可能）
- 快適な60fpsプレイ体験
- 新規プレイヤーが迷わない導線

---

### Phase 5 (Future): Personal Camp（個人キャンプ - 拡張）

**Goal**: プレイヤー専用の装飾スペースで没入度向上

**Entry Criteria**: Phase 4 完了 + ユーザーフィードバック収集

**Tasks**:

- [ ] **個人区画シーン作成** (depends on: [PersistenceManager])
  - Acceptance criteria:
    - PersonalCamp_Templateシーン作成
    - ポータルでシーン遷移（VRCSceneDescriptorの設定）
    - 家具配置データをPlayerDataから読み込み
  - Test strategy:
    - 統合: メインキャンプ→ポータル→個人区画→復帰

- [ ] **家具配置システム** (depends on: [PersistenceManager, ShopUI])
  - Acceptance criteria:
    - 購入済み家具をPickupで配置
    - グリッド吸着 or 自由配置
    - 配置データをJSON文字列化してPlayerData保存
    - 容量チェック（100KB制限）
  - Test strategy:
    - 単体: JSON変換の正確性
    - 統合: 家具10個配置→保存→再ログイン→復元確認

**Exit Criteria**:
- 個人キャンプが正常に動作
- 家具配置が永続化される
- PlayerData容量が100KB以内

**Delivers**:
- プレイヤーの「自分らしさ」表現
- 長期継続プレイのモチベーション向上

---

## 🧪 テスト戦略（Test Strategy）

### Test Pyramid

```
        /\
       /E2E\       ← 10% (End-to-end, 実機テスト、遅い)
      /------\
     /Integration\ ← 30% (モジュール間連携、Build & Test)
    /------------\
   /  Unit Tests  \ ← 60% (高速、単一スクリプト、決定的)
  /----------------\
```

### Coverage Requirements

VRChatワールド（Udon）は従来の単体テストフレームワーク（NUnit等）が使いにくいため、**手動テスト + チェックリスト方式**を採用

- **ロジック部分の単体テスト**: 60%
  - 計算式（ダメージ、XP、レベルなど）はUnity Editor上でテスト関数実行
  - 境界値テスト（Lv1→2、HP=0など）を重点的に
- **統合テスト**: 30%
  - Unity Editor Play Mode + VRChat Build & Test
  - 2クライアント同期テスト（別PC or Editor + Quest実機）
- **E2Eテスト**: 10%
  - VRChat本番環境アップロード（Private World）
  - 5人でのテストプレイ（会話しながらの実践テスト）

### Critical Test Scenarios

#### Module: Woodcutting（伐採）

**Happy path**:
- プレイヤーが木を10回叩く → HP=0 → 倒木アニメーション → 丸太3本生成
- Expected: スムーズな演出、ラグなし

**Edge cases**:
- 木のHP=1時に2人が同時攻撃 → 協力ボーナス発動？
- Expected: HP=0を複数回判定しない（フラグ管理）

**Error cases**:
- 倒木中に再度攻撃
- Expected: 無視（状態が「Standing」以外は攻撃不可）

**Integration points**:
- TreeController ↔ SkillManager（XP加算）
- Expected: 木1本伐採 = Woodcutting +10 XP

---

#### Module: Carrying（運搬）

**Happy path**:
- プレイヤーが小丸太をPickup → 30m運搬 → Dropゾーンで離す → Carrying +15 XP
- Expected: 移動速度デバフ適用、XP正確

**Edge cases**:
- 太丸太を1人だけでPickup → ほぼ動かない
- Expected: 移動速度 = 0.01倍（ほぼ静止）

**Error cases**:
- 丸太を持ったまま別シーンに移動
- Expected: 自動Drop処理（シーン遷移時）

**Integration points**:
- CarryingTracker ↔ SkillManager（XP加算）
- Expected: 距離100m運搬 = Carrying +50 XP

---

#### Module: Progression（成長）

**Happy path**:
- Woodcutting Lv1、XP=90 → 木1本伐採（+10 XP） → Lv2昇格 → ダメージ1.1倍
- Expected: レベルアップ演出、即座にパッシブ反映

**Edge cases**:
- Lv19、XP=9990 → +10 XP → Lv20（最大）
- Expected: Lv20以降はXP加算されるが、レベルアップなし

**Error cases**:
- PlayerData破損（XP=負値）
- Expected: デフォルト値（Lv1、XP=0）で復旧

**Integration points**:
- SkillManager ↔ PersistenceManager（保存・読み込み）
- Expected: ログアウト→再ログイン→スキルレベル維持

---

#### Module: Economy（経済）

**Happy path**:
- きこりコイン500枚 → 斧スキン購入（300枚） → 残高200枚
- Expected: コイン減算、装備可能化

**Edge cases**:
- コイン100枚で300枚のアイテム購入試行
- Expected: エラー表示「コイン不足」、購入不可

**Error cases**:
- 同時に2つのアイテム購入（ダブルクリック）
- Expected: 1つ目のみ処理、2つ目は残高不足で失敗

**Integration points**:
- ShopUI ↔ CoinManager ↔ PersistenceManager
- Expected: 購入→PlayerData["UnlockedCosmetics"]更新→再ログイン→装備維持

---

#### Module: Social（ソーシャル）

**Happy path**:
- 総伐採100本達成 → 実績「きこり見習い」解禁 → トロフィー表示 → コイン+100
- Expected: 解禁演出、報酬付与

**Edge cases**:
- 実績条件達成後、即座に2つ目の実績条件達成
- Expected: 両方とも正しく解禁（並列処理）

**Error cases**:
- トロフィーウォールに20プレイヤー参加（オブジェクト数大）
- Expected: Quest環境で60fps維持（LOD適用）

**Integration points**:
- AchievementChecker ↔ TrophyWall ↔ CoinManager
- Expected: 実績解禁→トロフィー表示→コイン加算

---

### Test Generation Guidelines（Udon特有の注意点）

1. **ネットワーク同期テスト**
   - Unity Editor + Build & Testで2クライアント必須
   - UdonSynced変数の同期遅延（100〜300ms）を考慮

2. **Persistenceテスト**
   - PlayerDataの保存は非同期（即座に反映されない場合あり）
   - 容量監視APIで100KB超過を確認

3. **パフォーマンステスト**
   - Quest 2実機で必ずテスト（EditorはPC性能で動くため当てにならない）
   - VRChat Profilerで「Udon Execution Time」を確認（< 5ms目標）

4. **境界値テスト**
   - レベルアップ境界（Lv1→2、Lv19→20）
   - HP=0（倒木判定）
   - コイン残高=0（購入不可）

5. **リグレッションテスト**
   - SDK更新時（3.9.0 → 3.10.0など）
   - Unity更新時（2022.3.22f1 → 次期LTS）

---

#### Module: Combat（戦闘・防衛）

**Happy path**:
- 60分経過 → 警告音 → 30秒後に熊出現 → プレイヤーが木の槍で攻撃 → 熊HP=0 → 撃退成功 → 熊の毛皮+200コイン獲得
- Expected: スムーズな演出、報酬付与、Combat +100 XP

**Edge cases**:
- 熊襲撃中にプレイヤー全員がキャンプ外へ逃走 → 3分経過で熊撤退
- Expected: 報酬なし、ダメージなし

- 熊のHP=1時に2人が同時攻撃 → 協力撃退ボーナス？
- Expected: 両者にCombat XP付与（二重判定防止）

**Error cases**:
- 熊襲撃中にインスタンスリセット（Master Client退出）
- Expected: 新Master Clientに熊の状態引き継ぎ（UdonSynced）

- プレイヤーがHP=0で倒された瞬間に熊も倒れた場合
- Expected: プレイヤーリスポーン、報酬は付与

**Integration points**:
- BearSpawner ↔ CampRankManager（熊レベル決定）
- Expected: Camp Rank 5 = 熊レベル5、HP=500

- WeaponController ↔ SkillManager（Combat XP加算）
- Expected: 熊撃退1回 = Combat +100 XP

- PlayerHealth ↔ DefenseEquipment（防具軽減）
- Expected: 鉄の鎧装備、30ダメージ → 21ダメージ受ける

- TrapSystem ↔ BearAI（罠ダメージ）
- Expected: 落とし穴発動 → 熊ダメージ30 + 3秒移動不可

---

## 🏛️ アーキテクチャ（Architecture）

### System Components

```
┌─────────────────────────────────────────────────────────────┐
│                       VRChat Client                          │
│  (Unity 2022.3.22f1 + SDK 3.9.0 + UdonSharp 1.1.9)          │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                   Udon Runtime Layer                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ GameManager  │  │ NetworkSync  │  │ Persistence  │      │
│  │ (Singleton)  │  │ (UdonSynced) │  │  Manager     │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        ▼                   ▼                   ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ Woodcutting  │  │  Carrying    │  │ Processing   │
│  Module      │  │   Module     │  │   Module     │
└──────────────┘  └──────────────┘  └──────────────┘
        │                   │                   │
        └───────────────────┼───────────────────┘
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                  Progression & Economy Layer                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ SkillManager │  │ CoinManager  │  │ CampRank     │      │
│  │ (PlayerData) │  │ (PlayerData) │  │ (Synced)     │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                     Social & UI Layer                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ Achievement  │  │ Leaderboard  │  │  HUDManager  │      │
│  │   Checker    │  │  (Synced)    │  │   (Canvas)   │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

### Data Models

#### PlayerData（100KB制限）

```json
{
  "Woodcutting_Level": 5,
  "Woodcutting_XP": 450,
  "Carrying_Level": 3,
  "Carrying_XP": 180,
  "Crafting_Level": 2,
  "Crafting_XP": 90,
  "Combat_Level": 4,
  "Combat_XP": 320,
  "Combat_Specialization": "offense",   // "offense" or "defense"（Lv6分岐）
  "WoodcutterCoins": 1200,
  "UnlockedCosmetics": [1, 3, 7, 12],   // アイテムID配列
  "UnlockedWeapons": [1, 2, 5],         // 武器ID配列
  "UnlockedArmor": [1, 3],              // 防具ID配列
  "EquippedWeapon": 5,                  // 現在装備中の武器ID
  "EquippedArmor": 3,                   // 現在装備中の防具ID
  "Achievements": [1, 2, 5, 10],        // 実績ID配列
  "TotalTreesCut": 350,
  "TotalDistance": 5000.5,              // メートル
  "TotalBearsDefeated": 12,             // 熊撃退回数
  "BearFurCount": 8,                    // 熊の毛皮所持数
  "LastVisit": "2025-11-15T10:30:00Z",
  "PersonalCampData": "{...}"           // JSON文字列（v1.1以降）
}
```

**容量見積もり**:
- スキルデータ: 8個 × 8バイト = 64バイト（Combat追加）
- コイン: 4バイト
- 熊の毛皮: 4バイト
- 配列: 各要素4バイト × 70個想定 = 280バイト（武器・防具追加）
- 文字列: 150バイト（Specialization追加）
- **合計: 約500バイト（圧縮前）** → 100KB制限に余裕あり

#### UdonSynced Variables（インスタンス共有）

```csharp
[UdonSynced] int campRank = 1;
[UdonSynced] int campXP = 0;
[UdonSynced] int campWoodPoints = 0;
[UdonSynced] int[] playerCutCounts = new int[20];  // プレイヤー別伐採数
[UdonSynced] string[] playerNames = new string[20];

// Combat関連
[UdonSynced] bool bearRaidActive = false;          // 熊襲撃中フラグ
[UdonSynced] int bearLevel = 1;                    // 現在の熊レベル
[UdonSynced] int bearHP = 100;                     // 熊の現在HP
[UdonSynced] Vector3 bearPosition;                 // 熊の位置（同期）
[UdonSynced] int totalBearsDefeatedToday = 0;      // 今日の熊撃退数（インスタンス）
```

**帯域制限**:
- 全Behaviourで10KB/秒以下
- Continuous Syncは頻繁な更新用（200バイト/回）
- Manual Syncは低頻度用（280KB/回だが頻度制限）

---

### Technology Stack

| レイヤー | 技術 | バージョン | 用途 |
|---------|------|----------|------|
| **開発環境** | Unity | 2022.3.22f1 | ワールド開発エンジン |
| **SDK** | VRChat SDK | 3.9.0 | VRChat統合 |
| **スクリプト** | UdonSharp | 1.1.9 | C#ライクなUdon記述 |
| **同期** | UdonSynced | SDK標準 | ネットワーク変数同期 |
| **永続化** | PlayerData API | SDK 3.7.4+ | プレイヤーデータ保存 |
| **UI** | Unity Canvas | 標準 | ワールドスペースUI |
| **3Dモデル** | Blender | 3.6+ | 木、丸太、設備モデリング |
| **テクスチャ** | Substance Painter | - | Quest対応1K以下 |
| **サウンド** | Audacity | - | 伐採音、BGM編集 |

---

### Key Design Decisions

#### Decision: UdonSharp採用（Udon Graph不使用）

**Rationale**:
- コード可読性: C#ライクな記述でメンテナンス容易
- バージョン管理: テキストファイルでGit管理可能
- 開発速度: Udon Graphのノード接続より高速

**Trade-offs**:
- UdonSharpはUdon Graphの上位レイヤー（若干のオーバーヘッド）
- 一部のUdon機能にアクセス困難（Soba待ち）

**Alternatives considered**:
- Udon Graph: ビジュアルスクリプティングだが大規模開発に不向き
- Soba（次世代）: まだ発表段階、2025年後半リリース予定

---

#### Decision: PlayerData（個人データ）とUdonSynced（共有データ）の分離

**Rationale**:
- スキル・コインは個人成長 → PlayerDataで永続化
- Camp Rankはインスタンス共有 → UdonSyncedで同期（非永続）
- データ容量節約（PlayerData 100KB制限）

**Trade-offs**:
- Camp Rankは毎回リセット → 「今日のキャンプ」感はあるが、長期育成感は薄い
- v1.1で「グローバルCamp Rank」を検討可能（別のPlayerDataキーで管理）

**Alternatives considered**:
- 全データをPlayerData保存 → 容量逼迫、インスタンス間の整合性問題
- 全データをUdonSynced → ログアウト後にデータ消失

---

#### Decision: SOLID原則の適用（VRChatワールドでの解釈）

**Single Responsibility**:
- SkillManagerはXP管理のみ、効果計算はSkillEffectsに委譲
- CoinManagerは通貨管理のみ、ショップUIはShopUIに委譲

**Open/Closed**:
- 新スキル追加はenum拡張のみ、既存コード変更不要
- 新レシピ追加はScriptableObjectで管理、コード変更不要

**Liskov Substitution**:
- LogPickupを継承したHeavyLogPickupでも同じインターフェース
- ISkillManagerインターフェースで抽象化（テスト時のモック可能）

**Interface Segregation**:
- AxeInteractionは攻撃判定のみ、音・エフェクトは別モジュール
- IPurchasableインターフェースで購入処理を統一

**Dependency Inversion**:
- TreeControllerはISkillManagerに依存、具体実装に依存しない
- ShopUIはICoinManagerに依存

**Trade-offs**:
- Udonは完全なインターフェース未対応 → 抽象クラス or プロトコル風運用
- 過度な抽象化はUdonのパフォーマンス低下 → 必要最小限に

---

#### Decision: Quest対応の最適化戦略

**Rationale**:
- VRChatユーザーの約30%がQuest利用（推定）
- Quest非対応は大幅なユーザー損失

**最適化手法**:
1. **ポリゴン削減**
   - LOD（Level of Detail）自動生成（Unity Pro Builder）
   - 遠景の木はビルボード化
2. **テクスチャ圧縮**
   - 1024x1024以下、ASTC 6x6圧縮
   - アトラスパッキングでマテリアル削減
3. **Lightmap焼き**
   - リアルタイムライトを削減、事前焼きで負荷軽減
4. **Udon最適化**
   - Updateループ削減、タイマーベースに変更
   - GetComponentのキャッシング

**Trade-offs**:
- PC版のビジュアル品質がQuest制約で制限される
- 開発時間増加（2プラットフォーム対応）

**Alternatives considered**:
- PC専用ワールド → ユーザー層が狭まる
- Quest専用ワールド → PC勢が満足できない品質

---

#### Decision: インスタンスリセット型Camp Rank（非永続）

**Rationale**:
- 毎回「今日のキャンプ」をみんなで育てる楽しさ
- PlayerData容量節約（Camp Rankを保存しない）
- 新規参加者も平等にスタート

**Trade-offs**:
- 長期育成感は薄い
- 同じプレイヤーが何度も同じキャンプに来ても、毎回Rank1からスタート

**Alternatives considered**:
- グローバルCamp Rank（全インスタンス共有） → 実装複雑、Persistence容量逼迫
- プレイヤーごとのCamp Rank → 「共同育成」感が失われる

---

## 📚 付録（Appendix）

### References

- **VRChat公式ドキュメント**
  - [creators.vrchat.com](https://creators.vrchat.com/)
  - [SDK 3.9.0 Release Notes](https://creators.vrchat.com/releases/release-3-9-0/)
  - [Persistence API](https://creators.vrchat.com/worlds/udon/persistence/)
  - [Quest Content Optimization](https://creators.vrchat.com/platforms/android/quest-content-optimization/)

- **技術参考**
  - [UdonSharp Performance Tips](https://creators.vrchat.com/worlds/udon/udonsharp/performance-tips/)
  - [Network Details](https://creators.vrchat.com/worlds/udon/networking/network-details/)

- **ゲームデザイン参考**
  - Valheim Wiki: [Wood Cutting](https://valheim.fandom.com/wiki/Wood_cutting)
  - Palia Developer Insights: [Cozy MMO Design](https://www.pcgamer.com/palia-interview/)
  - Deep Rock Galactic: [Cosmetic System](https://www.reddit.com/r/DeepRockGalactic/comments/cosmetics/)

- **RPG方式**
  - Microsoft Research: Repository Planning Graph (RPG) Methodology

---

### Glossary

| 用語 | 説明 |
|------|------|
| **Udon** | VRChatのワールド用スクリプト言語（低レベル） |
| **UdonSharp** | C#ライクなUdon記述ツール（高レベル） |
| **Soba** | 次世代VRChatスクリプト言語（CILベース、2025年後半予定） |
| **PlayerData** | プレイヤーごとの永続データ（100KB制限） |
| **UdonSynced** | インスタンス内で同期される変数 |
| **Camp Rank** | インスタンス共有の拠点レベル（非永続） |
| **Persistence** | VRChatのデータ永続化API（SDK 3.7.4〜） |
| **Quest** | Meta Quest 2/3（Androidベース、性能制約大） |
| **VCC** | VRChat Creator Companion（SDK管理ツール） |
| **Cozy MMO** | 戦闘なし、まったり系MMO（Paliaなど） |
| **SOLID** | オブジェクト指向設計の5原則 |
| **Combat スキル** | 戦闘行動で成長する第4のライフスキル |
| **熊襲撃** | 60分ごとに発生するランダムイベント（緊張感追加） |
| **熊の毛皮** | 熊撃退で入手できる特殊素材（防具クラフト用） |
| **Tier 1/2/3** | 武器・防具のアップグレード段階（木→鉄→鋼） |

---

### Open Questions

1. **個人キャンプの実装タイミング**
   - v1では見送り、ユーザーフィードバック次第で v1.1 実装
   - PlayerData容量が十分か検証必要

2. **グローバルランキングの実装可否**
   - Persistence容量制限で全インスタンス横断は困難
   - 外部API（VRChat Player API + 外部DB）活用を検討

3. **Soba移行の判断基準**
   - 2025年後半にSoba正式リリース予定
   - パフォーマンス改善が顕著ならUdonSharpから移行検討

4. **斧スイングの入力方法**
   - PC: マウスクリック
   - VR: トリガー
   - Quest: トリガー
   - → 各デバイスで操作感をテストプレイで検証

5. **丸太の物理演算範囲**
   - 完全な物理シミュは負荷大
   - アニメーションベースの倒木 + 簡易物理（丸太転がり）
   - → Quest60fps維持を最優先

---

### Task Master Integration Note

このPRDは**Repository Planning Graph (RPG)方式**に準拠しています。

**Task Masterでの利用方法**:
```bash
task-master parse-prd docs/PRD.md
```

**依存グラフ**:
- Phase 0 → Phase 1 → Phase 2 → Phase 3 → Phase 4 → Phase 5
- 各フェーズ内のタスクは並列実行可能
- 依存関係は明示的に記載（`depends on: [...]`）

**タスク優先度**:
- Phase 0（基盤）: 最優先
- Phase 1（成長）: 高優先
- Phase 2（ゲームループ）: 高優先
- Phase 3（ソーシャル）: 中優先
- Phase 4（最適化）: 中優先
- Phase 5（個人キャンプ）: 低優先（拡張要素）

---

## ✅ PRD承認

| 役割 | 承認者 | 日付 | 備考 |
|------|--------|------|------|
| **プロダクトオーナー** | [承認待ち] | - | ユーザー体験・KPI承認 |
| **シニアエンジニア** | [承認待ち] | - | 技術実装可能性承認 |
| **アーキテクト** | [承認待ち] | - | SOLID原則・依存関係承認 |
| **Questテスター** | [承認待ち] | - | Quest対応戦略承認 |

---

**PRD Version**: 1.0
**Next Review**: v1実装完了後（Phase 4完了時）
**Document Owner**: VRChatワールド開発チーム
